/**
 * The maximum number of records that Supabase will return, this is the default.
 * This can be updated in the Supabase dashboard under the "Settings" > API section.
 */
export const SUPABASE_QUERY_LIMIT = 1000;

/**
 * The number of transactions to retrieve per request. The maximum is 500.
 */
export const PLAID_SYNC_BATCH_SIZE = 500;
